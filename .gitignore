# deps
node_modules/
.pnpm-store/

# build
dist/
build/
.vite/

# env / secrets (NUNCA commitar)
.env
.env.*
!.env.example
# template de produção (só placeholders) — deve ser versionado
!.env.production.example

# prisma
*.db

# logs
*.log
logs/

# os/editor
.DS_Store
.vscode/
.idea/

# docker volumes locais
pgdata/
redisdata/
backups/
uploads/

# backend runtime/debug output (nunca versionar)
apps/backend/.be.*
apps/backend/.studio*
apps/backend/.chk/
apps/backend/err.txt
*.tmp

# TypeScript incremental build cache (artefato de build, não versionar)
*.tsbuildinfo

waha-sessions/

# graphify (skill de terceiros — grafo local, nunca versionar)
graphify-out/
