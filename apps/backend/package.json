{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Nexa Backend (NestJS) — fundação Sprint 1 (Prisma). NestJS entra no Sprint 2.",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:seed": "ts-node prisma/seed.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}
